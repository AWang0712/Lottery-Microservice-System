/**
 *
 * {infrastructure}
 *
 * Basic services are located in the infrastructure layer.
 * It provides resource services (such as database, cache, etc.) for each layer,
 * realizes decoupling of each layer, and reduces the impact of external resource changes on business logic.
 *
 */